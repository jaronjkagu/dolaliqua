import requests

# Create a session object
session = requests.Session()

# Send a GET request using the session object
response = session.get('https://www.example.com Print the response content
print(response.content)
